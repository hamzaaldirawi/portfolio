import { 
    LoadingContainer,
    LoadingCircleHolder,
    LoadingCircle
} from './styles';

const Loading = () => (
    <LoadingContainer>
        <LoadingCircleHolder>
            <LoadingCircle />
        </LoadingCircleHolder>
    </LoadingContainer> 
)

export default Loading;